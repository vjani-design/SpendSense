<?xml version="1.0" encoding="utf-8"?>
<paths>

    <!-- Cache files (PDF temporary storage) -->
    <cache-path
        name="cache"
        path="." />

    <!-- External app-specific files -->
    <external-files-path
        name="external_files"
        path="." />

</paths>